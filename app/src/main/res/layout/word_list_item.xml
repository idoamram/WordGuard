<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="4dp"
    android:layout_marginTop="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/left_image"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:background="@color/swipe_done_green"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true"
            app:srcCompat="@drawable/ic_done_white"
            android:scaleType="centerInside"
            android:tint="@android:color/white" />

        <ImageView
            android:id="@+id/right_image"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:scaleType="centerInside"
            app:srcCompat="@drawable/ic_delete_white"
            android:background="@color/swipe_delete_red" />

        <TextView
            android:background="@android:color/white"
            android:id="@+id/share_link_name_text"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:gravity="center_vertical"
            android:padding="20dp"
            android:textColor="@android:color/black"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Link name but really long, I mean really really long"
            android:textSize="18sp" />

    </RelativeLayout>
</android.support.v7.widget.CardView>
